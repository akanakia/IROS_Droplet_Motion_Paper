\documentclass[main.tex]{subfiles}

\begin{document}

\begin{tikzpicture}[scale=1,auto]

\def\rectSize{2}
\def\platformHeight{0.2}
\def\supportWidth{0.2}
\def\motArmLength{1.5}
\def\motTheta{-20}
\def\motMassRad{0.5}
\def\motArcTheta{45}
\pgfmathsetmacro{\midwayArc}{(\motArcTheta/2)+\motTheta}

\draw[draw=none] (-\rectSize,-\rectSize) rectangle (\rectSize,\rectSize);

\draw[thick] (\supportWidth,0) --  (\supportWidth,-\rectSize+\platformHeight) -- (\rectSize,-\rectSize+\platformHeight) -- (\rectSize,-\rectSize) -- (-\rectSize,-\rectSize) -- 
		(-\rectSize,-\rectSize+\platformHeight) --  (-\supportWidth,-\rectSize+\platformHeight) -- (-\supportWidth,0) arc (180:0:\supportWidth);

\draw[thick,fill=white] (\motTheta:\motArmLength) circle (\motMassRad);

\draw[thick, double distance=1pt,round cap-triangle 90 cap] (0,0) -- (\motTheta:\motArmLength);

\draw[-latex] (\motTheta:\motArmLength) arc (\motTheta:\motTheta+\motArcTheta:\motArmLength);
\draw[black!50] (0,0) -- (\motTheta+\motArcTheta:\motArmLength+0.3);
\node[fill=white,circle,inner sep=1.5] at (\midwayArc:\motArmLength+0.2) {$\omega$};

\end{tikzpicture}



\end{document}